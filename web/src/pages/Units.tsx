import { useQuery } from '@tanstack/react-query'; import { api } from '../api';
export function Units(){const {data=[]}=useQuery({queryKey:['units'],queryFn:()=>api.get('/inventory/units').then(r=>r.data)});return <><div className="section-title"><h2>Units</h2><button className="btn btn-primary">Add unit</button></div><section className="panel table-responsive"><table className="table align-middle"><thead><tr><th>Unit</th><th>Area</th><th>Landlord</th><th>Spaces</th><th>Status</th></tr></thead><tbody>{data.map((u:any)=><tr key={u.id}><td><strong>{u.name}</strong><small>{u.building}</small></td><td>{u.area}</td><td>{u.landlord?.name??'—'}</td><td>{u.spaces.length}</td><td><span className={`badge ${u.active?'text-bg-success':'text-bg-secondary'}`}>{u.active?'Active':'Information complete'}</span></td></tr>)}</tbody></table>{!data.length&&<div className="empty-state"><i className="bi bi-buildings"/><strong>No units yet</strong><span>Add the first property you manage.</span></div>}</section></>}
