import { Navigate,NavLink,Outlet,Route,Routes } from 'react-router-dom';
import { useAuth } from './auth'; import { Login } from './pages/Login'; import { Dashboard } from './pages/Dashboard'; import { Units } from './pages/Units'; import { Contracts } from './pages/Contracts'; import { Requests } from './pages/Requests';
function Guard(){const {token}=useAuth();return token?<Outlet/>:<Navigate to="/login" replace/>}
function Layout(){const {logout}=useAuth();return <div className="app-shell"><aside className="sidebar"><div className="brand">Leaseflow</div><nav><NavLink to="/">Dashboard</NavLink><NavLink to="/units">Units</NavLink><NavLink to="/contracts">Contracts</NavLink><NavLink to="/requests">Requests</NavLink></nav><button className="btn btn-outline-light btn-sm mt-auto" onClick={logout}>Sign out</button></aside><main><header><div><h1>Accommodation operations</h1><p>Units, tenants, payments and requests</p></div><span className="badge text-bg-success">Operational</span></header><div className="content"><Outlet/></div></main></div>}
export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route element={<Guard/>}><Route element={<Layout/>}><Route index element={<Dashboard/>}/><Route path="units" element={<Units/>}/><Route path="contracts" element={<Contracts/>}/><Route path="requests" element={<Requests/>}/></Route></Route></Routes>}
